html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Киберпанк | Смертников</title>
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <!-- Анимированный красный туман -->
    <div class="blood-fog"></div>
    
    <!-- Анимированные красные кресты -->
    <div class="blood-crosses"></div>
    
    <!-- Анимированный фон -->
    <div class="cyber-background"></div>
    
    <!-- Главный контейнер -->
    <main class="container">
        <!-- Аватар и информация -->
        <section class="profile-section">
            <div class="avatar-container">
                <img src="img/avatar.jpg" alt="Смертников" class="avatar" id="avatar">
                <div class="avatar-blood"></div>
                <div class="blood-drip"></div>
            </div>
            
            <div class="profile-info">
                <h1 class="nickname">Александр Смертников</h1>
                <p class="username">@ownerTwilight</p>
                <div class="status">
                    <div class="status-dot"></div>
                    <span class="status-text">ТГШНЫЙ РАЗНОГЛАСИЕ, ИЛЮША ПСИХОПАТОВ</span>
                </div>
                
                <div class="price-text">
                    <div class="price-line">КАСТА ФУЛЛ</div>
                    <div class="price-details">
                        ПРАЙС - ДЕФ МЕСЯЦ 550|3 МЕСЯЦА 1250|6 МЕСЯЦЕВ 1700|1 ГОД 2500|НАВСЕГДА 5К|ДОКС 600
                    </div>
                </div>
            </div>
        </section>

        <!-- Кнопки на одном уровне -->
        <section class="buttons-section">
            <div class="buttons-row">
                <a href="https://t.me/+yuLEBTEKqdU1ZTQ1" class="blood-btn channel-btn" target="_blank">
                    <div class="btn-content">
                        <i class="fab fa-telegram"></i>
                        <span class="btn-text">МОЙ КАНАЛ</span>
                    </div>
                    <div class="blood-drip-btn"></div>
                    <div class="btn-pulse"></div>
                    <div class="btn-glow-red"></div>
                </a>

                <a href="https://t.me/+PCSL9n5EWTcyYWQ1" class="blood-btn chat-btn" target="_blank">
                    <div class="btn-content">
                        <i class="fas fa-comments"></i>
                        <span class="btn-text">МОЙ ЧАТ</span>
                    </div>
                    <div class="blood-drip-btn"></div>
                    <div class="btn-pulse"></div>
                    <div class="btn-glow-red"></div>
                </a>
            </div>
        </section>
    </main>

    <!-- Аудио для эффектов -->
    <audio id="hover-sound" preload="auto">
        <source src="https://assets.mixkit.co/sfx/preview/mixkit-sci-fi-horror-alarm-905.mp3" type="audio/mpeg">
    </audio>

    <script src="script.js"></script>
</body>
</html>
style.css (обновлённый)
css
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Courier New', 'Consolas', monospace;
}

body {
    background: #0a0000;
    color: #ff002b;
    min-height: 100vh;
    overflow-x: hidden;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
}

/* Кровавый фон */
.cyber-background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
        radial-gradient(circle at 20% 30%, rgba(139, 0, 0, 0.3) 0%, transparent 50%),
        radial-gradient(circle at 80% 70%, rgba(255, 0, 0, 0.2) 0%, transparent 50%),
        linear-gradient(45deg, #000000 0%, #1a0000 50%, #000000 100%);
    z-index: 1;
    animation: bloodPulse 8s infinite alternate;
}

@keyframes bloodPulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.8; }
}

/* Кровавый туман */
.blood-fog {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
        url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><filter id="turbulence"><feTurbulence baseFrequency="0.02" numOctaves="3" seed="1"/><feDisplacementMap in="SourceGraphic" scale="3"/></filter><rect width="100" height="100" filter="url(%23turbulence)" fill="rgba(255,0,0,0.1)"/></svg>');
    background-size: 200px;
    opacity: 0.6;
    animation: fogMove 30s linear infinite;
    z-index: 2;
    pointer-events: none;
}

@keyframes fogMove {
    0% { background-position: 0 0; }
    100% { background-position: 200px 200px; }
}

/* Анимированные красные кресты */
.blood-crosses {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
        url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100"><path d="M50,10 L50,90 M10,50 L90,50" stroke="rgba(255,0,0,0.1)" stroke-width="2"/></svg>'),
        url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" width="150" height="150"><path d="M75,15 L75,135 M15,75 L135,75" stroke="rgba(139,0,0,0.08)" stroke-width="3"/></svg>');
    background-size: 100px 100px, 150px 150px;
    opacity: 0.4;
    animation: crossFloat 60s linear infinite;
    z-index: 3;
    pointer-events: none;
}

@keyframes crossFloat {
    0% { background-position: 0 0, 100px 100px; }
    100% { background-position: 100px 100px, 0 0; }
}

/* Основной контейнер */
.container {
    position: relative;
    z-index: 4;
    max-width: 1200px;
    margin: 0 auto;
    padding: 40px 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 100vh;
    justify-content: center;
}

/* Секция профиля */
.profile-section {
    text-align: center;
    margin-bottom: 60px;
    width: 100%;
    max-width: 800px;
    padding: 40px;
    background: rgba(10, 0, 0, 0.85);
    border: 2px solid rgba(255, 0, 0, 0.5);
    border-radius: 0;
    box-shadow: 
        0 0 50px rgba(255, 0, 0, 0.4),
        inset 0 0 30px rgba(139, 0, 0, 0.3);
    position: relative;
    overflow: hidden;
}

.profile-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, 
        transparent, 
        rgba(255, 0, 0, 0.2), 
        transparent);
    animation: scanline 3s linear infinite;
}

@keyframes scanline {
    0% { left: -100%; }
    100% { left: 100%; }
}

.avatar-container {
    position: relative;
    width: 200px;
    height: 200px;
    margin: 0 auto 30px;
    border: 3px solid #ff002b;
    border-radius: 50%;
    overflow: hidden;
    box-shadow: 0 0 40px rgba(255, 0, 0, 0.6);
}

.avatar {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: sepia(0.3) contrast(1.2) saturate(1.5);
    transition: all 0.5s ease;
}

.avatar:hover {
    transform: scale(1.05);
    filter: sepia(0.5) contrast(1.4) saturate(2);
}

.avatar-blood {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 30% 30%, 
        transparent 40%, 
        rgba(255, 0, 0, 0.3) 70%);
    z-index: 1;
    pointer-events: none;
}

.blood-drip {
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 2px;
    height: 30px;
    background: linear-gradient(to bottom, #ff002b, transparent);
    animation: drip 3s infinite;
}

@keyframes drip {
    0%, 100% { height: 30px; opacity: 0.8; }
    50% { height: 50px; opacity: 1; }
}

.profile-info {
    margin-top: 20px;
}

.nickname {
    font-size: 3rem;
    font-weight: bold;
    color: #ff002b;
    text-shadow: 
        0 0 20px #ff002b,
        0 0 40px #ff002b,
        0 0 60px #ff0000;
    letter-spacing: 3px;
    margin-bottom: 10px;
    text-transform: uppercase;
    animation: textGlitch 5s infinite;
}

@keyframes textGlitch {
    0%, 100% { transform: translateX(0); }
    95% { transform: translateX(0); }
    96% { transform: translateX(-2px); }
    97% { transform: translateX(2px); }
    98% { transform: translateX(-2px); }
    99% { transform: translateX(2px); }
}

.username {
    font-size: 1.5rem;
    color: #ff6666;
    letter-spacing: 4px;
    margin-bottom: 20px;
    font-weight: 300;
    text-transform: uppercase;
}

.status {
    display: inline-flex;
    align-items: center;
    gap: 15px;
    padding: 10px 30px;
    background: rgba(20, 0, 0, 0.9);
    border: 1px solid #ff002b;
    border-radius: 0;
    margin-bottom: 20px;
}

.status-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: #ff002b;
    box-shadow: 0 0 15px #ff002b;
    animation: blink 1s infinite;
}

@keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
}

.status-text {
    color: #ff002b;
    font-size: 1rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    font-weight: bold;
}

.price-text {
    margin-top: 30px;
    padding: 20px;
    background: rgba(30, 0, 0, 0.8);
    border: 1px solid #ff002b;
    position: relative;
    overflow: hidden;
}

.price-line {
    color: #ff002b;
    font-size: 1.8rem;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 15px;
    text-shadow: 0 0 10px #ff002b;
}

.price-details {
    color: #ff6666;
    font-size: 1rem;
    line-height: 1.6;
    letter-spacing: 1px;
    text-transform: uppercase;
}

/* Кнопки на одном уровне */
.buttons-section {
    width: 100%;
    max-width: 900px;
    margin-top: 30px;
}

.buttons-row {
    display: flex;
    flex-direction: row;
    justify-content: center;
    gap: 40px;
    align-items: center;
    flex-wrap: wrap;
}

.blood-btn {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 280px;
    height: 280px;
    background: rgba(20, 0, 0, 0.9);
    border: 3px solid #ff002b;
    border-radius: 50%;
    text-decoration: none;
    color: #ff002b;
    transition: all 0.4s ease;
    overflow: hidden;
    cursor: pointer;
    box-shadow: 
        0 0 50px rgba(255, 0, 43, 0.5),
        inset 0 0 30px rgba(139, 0, 0, 0.4);
    animation: floatBtn 3s ease-in-out infinite alternate;
}

.channel-btn {
    animation-delay: 0s;
}

.chat-btn {
    animation-delay: 1.5s;
}

@keyframes floatBtn {
    0% {
        transform: translateY(0) rotate(0deg);
        box-shadow: 0 0 50px rgba(255, 0, 43, 0.5);
    }
    100% {
        transform: translateY(-20px) rotate(5deg);
        box-shadow: 0 0 80px rgba(255, 0, 43, 0.8);
    }
}

.blood-btn:hover {
    transform: scale(1.1) rotate(-5deg);
    border-color: #ff0000;
    box-shadow: 
        0 0 100px rgba(255, 0, 0, 0.9),
        inset 0 0 50px rgba(255, 0, 0, 0.5);
    background: rgba(30, 0, 0, 0.95);
    animation-play-state: paused;
}

.blood-drip-btn {
    position: absolute;
    bottom: 10px;
    left: 50%;
    transform: translateX(-50%);
    width: 3px;
    height: 30px;
    background: linear-gradient(to bottom, #ff002b, transparent);
    animation: dripBtn 2s infinite;
    opacity: 0.7;
}

.chat-btn .blood-drip-btn {
    animation-delay: 0.5s;
}

@keyframes dripBtn {
    0%, 100% { height: 30px; opacity: 0.7; }
    50% { height: 50px; opacity: 1; }
}

.btn-pulse {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(255, 0, 43, 0.3);
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0% { width: 0; height: 0; opacity: 1; }
    100% { width: 300px; height: 300px; opacity: 0; }
}

.btn-content {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
}

.blood-btn i {
    font-size: 4rem;
    color: #ff002b;
    transition: all 0.3s ease;
}

.blood-btn:hover i {
    color: #ffffff;
    transform: scale(1.3);
    text-shadow: 0 0 30px #ff002b;
    animation: iconBeat 0.5s ease infinite alternate;
}

@keyframes iconBeat {
    0% { transform: scale(1.3); }
    100% { transform: scale(1.5); }
}

.btn-text {
    font-size: 1.8rem;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 3px;
    color: #ff002b;
    transition: all 0.3s ease;
    text-align: center;
}

.blood-btn:hover .btn-text {
    color: #ffffff;
    letter-spacing: 4px;
    text-shadow: 0 0 15px #ff002b;
}

.btn-glow-red {
    position: absolute;
    top: -5px;
    left: -5px;
    right: -5px;
    bottom: -5px;
    border-radius: 50%;
    background: conic-gradient(
        from 0deg,
        transparent,
        rgba(255, 0, 43, 0.2),
        transparent
    );
    z-index: 1;
    opacity: 0;
    transition: opacity 0.3s ease;
}

.blood-btn:hover .btn-glow-red {
    opacity: 1;
    animation: rotateGlow 3s linear infinite;
}

@keyframes rotateGlow {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}

/* Анимация загрузки */
@keyframes fadeInBlood {
    from {
        opacity: 0;
        transform: translateY(30px);
        filter: brightness(0);
    }
    to {
        opacity: 1;
        transform: translateY(0);
        filter: brightness(1);
    }
}

.profile-section,
.buttons-section {
    animation: fadeInBlood 1s ease-out forwards;
}

.profile-section {
    animation-delay: 0.3s;
}

.buttons-section {
    animation-delay: 0.6s;
}

/* Адаптивность */
@media (max-width: 1024px) {
    .buttons-row {
        gap: 30px;
    }
    
    .blood-btn {
        width: 240px;
        height: 240px;
    }
}

@media (max-width: 768px) {
    .container {
        padding: 20px 15px;
        justify-content: flex-start;
        padding-top: 40px;
    }
    
    .profile-section {
        padding: 30px 20px;
        margin-bottom: 40px;
    }
    
    .avatar-container {
        width: 150px;
        height: 150px;
    }
    
    .nickname {
        font-size: 2.2rem;
    }
    
    .price-line {
        font-size: 1.4rem;
    }
    
    .price-details {
        font-size: 0.9rem;
    }
    
    .buttons-row {
        flex-direction: column;
        gap: 30px;
    }
    
    .blood-btn {
        width: 220px;
        height: 220px;
    }
    
    .blood-btn i {
        font-size: 3.5rem;
    }
    
    .btn-text {
        font-size: 1.5rem;
    }
}

@media (max-width: 480px) {
    .nickname {
        font-size: 1.8rem;
    }
    
    .username {
        font-size: 1.2rem;
    }
    
    .status-text {
        font-size: 0.9rem;
        text-align: center;
        line-height: 1.4;
    }
    
    .status {
        flex-direction: column;
        gap: 10px;
        padding: 15px;
    }
    
    .price-text {
        padding: 15px;
    }
    
    .blood-btn {
        width: 200px;
        height: 200px;
    }
    
    .blood-btn i {
        font-size: 3rem;
    }
    
    .btn-text {
        font-size: 1.3rem;
    }
}
script.js (обновлённый)
javascript
document.addEventListener('DOMContentLoaded', function() {
    console.log('🩸 Киберпанк Смертников активирован');
    
    initBloodEffects();
    initAvatarAnimation();
    initAudioEffects();
    initBloodCursor();
    createBloodRain();
    
    // Добавляем эффект мигания статуса
    const statusDot = document.querySelector('.status-dot');
    if (statusDot) {
        setInterval(() => {
            statusDot.style.boxShadow = `0 0 ${15 + Math.random() * 10}px #ff002b`;
        }, 1000);
    }
});

function initBloodEffects() {
    const buttons = document.querySelectorAll('.blood-btn');
    
    buttons.forEach(button => {
        button.addEventListener('mouseenter', function() {
            this.classList.add('active');
            createBloodParticles(this);
            playBloodSound();
            
            // Увеличиваем пульсацию
            const pulse = this.querySelector('.btn-pulse');
            if (pulse) {
                pulse.style.animation = 'pulse 1s infinite';
            }
        });
        
        button.addEventListener('mouseleave', function() {
            this.classList.remove('active');
            
            // Возвращаем обычную пульсацию
            const pulse = this.querySelector('.btn-pulse');
            if (pulse) {
                pulse.style.animation = 'pulse 2s infinite';
            }
        });
        
        button.addEventListener('click', function(e) {
            this.style.transform = 'scale(0.95) rotate(10deg)';
            setTimeout(() => {
                this.style.transform = '';
            }, 200);
            
            logBloodClick(this.href);
            
            // Создаём взрыв частиц при клике
            createBloodExplosion(this);
        });
    });
}

function initAvatarAnimation() {
    const avatar = document.getElementById('avatar');
    if (!avatar) return;
    
    avatar.addEventListener('mouseenter', function() {
        this.style.transition = 'transform 0.8s cubic-bezier(0.68, -0.55, 0.265, 1.55)';
        this.style.transform = 'rotate(5deg) scale(1.05)';
    });
    
    avatar.addEventListener('mouseleave', function() {
        this.style.transform = 'rotate(0deg) scale(1)';
    });
    
    setInterval(() => {
        const glow = document.querySelector('.avatar-blood');
        if (glow) {
            glow.style.background = `radial-gradient(circle at ${30 + Math.random() * 40}% ${30 + Math.random() * 40}%, 
                transparent 40%, 
                rgba(255, 0, 0, ${0.2 + Math.random() * 0.3}) 70%)`;
        }
    }, 3000);
}

function createBloodParticles(element) {
    const rect = element.getBoundingClientRect();
    const centerX = rect.left + rect.width / 2;
    const centerY = rect.top + rect.height / 2;
    const particles = 15;
    
    for (let i = 0; i < particles; i++) {
        const particle = document.createElement('div');
        particle.className = 'blood-particle';
        
        particle.style.position = 'fixed';
        particle.style.left = `${centerX}px`;
        particle.style.top = `${centerY}px`;
        particle.style.width = `${4 + Math.random() * 4}px`;
        particle.style.height = `${4 + Math.random() * 4}px`;
        particle.style.background = '#ff002b';
        particle.style.borderRadius = '50%';
        particle.style.pointerEvents = 'none';
        particle.style.zIndex = '1000';
        particle.style.boxShadow = '0 0 10px #ff002b';
        
        const angle = Math.random() * Math.PI * 2;
        const speed = 1 + Math.random() * 3;
        const distance = 60 + Math.random() * 80;
        
        particle.animate([
            {
                transform: `translate(0, 0) scale(1)`,
                opacity: 1
            },
            {
                transform: `translate(${Math.cos(angle) * distance}px, ${Math.sin(angle) * distance}px) scale(0)`,
                opacity: 0
            }
        ], {
            duration: 800 + Math.random() * 800,
            easing: 'cubic-bezier(0.215, 0.610, 0.355, 1)'
        });
        
        document.body.appendChild(particle);
        
        setTimeout(() => {
            if (particle.parentNode) {
                particle.parentNode.removeChild(particle);
            }
        }, 1500);
    }
}

function createBloodExplosion(element) {
    const rect = element.getBoundingClientRect();
    const centerX = rect.left + rect.width / 2;
    const centerY = rect.top + rect.height / 2;
    const particles = 30;
    
    for (let i = 0; i < particles; i++) {
        const particle = document.createElement('div');
        particle.className = 'blood-explosion';
        
        particle.style.position = 'fixed';
        particle.style.left = `${centerX}px`;
        particle.style.top = `${centerY}px`;
        particle.style.width = `${6 + Math.random() * 6}px`;
        particle.style.height = `${6 + Math.random() * 6}px`;
        particle.style.background = '#ff002b';
        particle.style.borderRadius = '50%';
        particle.style.pointerEvents = 'none';
        particle.style.zIndex = '1000';
        particle.style.boxShadow = '0 0 15px #ff002b';
        
        const angle = Math.random() * Math.PI * 2;
        const speed = 2 + Math.random() * 4;
        const distance = 100 + Math.random() * 150;
        
        particle.animate([
            {
                transform: `translate(0, 0) scale(1)`,
                opacity: 1
            },
            {
                transform: `translate(${Math.cos(angle) * distance}px, ${Math.sin(angle) * distance}px) scale(0)`,
                opacity: 0
            }
        ], {
            duration: 600 + Math.random() * 600,
            easing: 'cubic-bezier(0.215, 0.610, 0.355, 1)'
        });
        
        document.body.appendChild(particle);
        
        setTimeout(() => {
            if (particle.parentNode) {
                particle.parentNode.removeChild(particle);
            }
        }, 1200);
    }
}

function initAudioEffects() {
    try {
        if (typeof AudioContext !== 'undefined' || typeof webkitAudioContext !== 'undefined') {
            window.audioContext = new (AudioContext || webkitAudioContext)();
        }
    } catch (e) {
        console.log('Аудио не поддерживается');
    }
}

function playBloodSound() {
    const audio = document.getElementById('hover-sound');
    if (audio) {
        audio.currentTime = 0;
        audio.volume = 0.15;
        audio.play().catch(e => console.log('Автовоспроизведение заблокировано'));
    }
}

function initBloodCursor() {
    const cursor = document.createElement('div');
    cursor.id = 'blood-cursor';
    cursor.style.position = 'fixed';
    cursor.style.width = '24px';
    cursor.style.height = '24px';
    cursor.style.border = '2px solid #ff002b';
    cursor.style.borderRadius = '50%';
    cursor.style.pointerEvents = 'none';
    cursor.style.zIndex = '9999';
    cursor.style.transform = 'translate(-50%, -50%)';
    cursor.style.transition = 'width 0.2s, height 0.2s, border-color 0.2s';
    cursor.style.boxShadow = '0 0 15px #ff002b';
    document.body.appendChild(cursor);
    
    let mouseX = 0;
    let mouseY = 0;
    let cursorX = 0;
    let cursorY = 0;
    
    document.addEventListener('mousemove', (e) => {
        mouseX = e.clientX;
        mouseY = e.clientY;
    });
    
    function animateCursor() {
        cursorX += (mouseX - cursorX) * 0.15;
        cursorY += (mouseY - cursorY) * 0.15;
        
        cursor.style.left = `${cursorX}px`;
        cursor.style.top = `${cursorY}px`;
        
        requestAnimationFrame(animateCursor);
    }
    
    animateCursor();
    
    document.querySelectorAll('a, .blood-btn').forEach(el => {
        el.addEventListener('mouseenter', () => {
            cursor.style.width = '40px';
            cursor.style.height = '40px';
            cursor.style.borderColor = '#ffffff';
            cursor.style.boxShadow = '0 0 25px #ff002b';
            cursor.style.background = 'rgba(255, 0, 43, 0.1)';
        });
        
        el.addEventListener('mouseleave', () => {
            cursor.style.width = '24px';
            cursor.style.height = '24px';
            cursor.style.borderColor = '#ff002b';
            cursor.style.boxShadow = '0 0 15px #ff002b';
            cursor.style.background = 'transparent';
        });
    });
}

function logBloodClick(url) {
    console.log(`🩸 Кровавый клик: ${url}`);
    
    const clickEffect = document.createElement('div');
    clickEffect.style.position = 'fixed';
    clickEffect.style.top = '50%';
    clickEffect.style.left = '50%';
    clickEffect.style.transform = 'translate(-50%, -50%)';
    clickEffect.style.color = '#ff002b';
    clickEffect.style.fontSize = '3rem';
    clickEffect.style.fontWeight = 'bold';
    clickEffect.style.zIndex = '10000';
    clickEffect.style.pointerEvents = 'none';
    clickEffect.style.textShadow = '0 0 20px #ff002b';
    clickEffect.textContent = 'BLOOD';
    document.body.appendChild(clickEffect);
    
    clickEffect.animate([
        { opacity: 1, transform: 'translate(-50%, -50%) scale(1)' },
        { opacity: 0, transform: 'translate(-50%, -50%) scale(2)' }
    ], {
        duration: 800,
        easing: 'ease-out'
    });
    
    setTimeout(() => {
        if (clickEffect.parentNode) {
            clickEffect.parentNode.removeChild(clickEffect);
        }
    }, 1000);
}

function createBloodRain() {
    setInterval(() => {
        const drop = document.createElement('div');
        drop.style.position = 'fixed';
        drop.style.top = '-10px';
        drop.style.left = `${Math.random() * 100}vw`;
        drop.style.width = `${1 + Math.random() * 2}px`;
        drop.style.height = `${20 + Math.random() * 30}px`;
        drop.style.background = 'linear-gradient(to bottom, #ff002b, transparent)';
        drop.style.pointerEvents = 'none';
        drop.style.zIndex = '3';
        document.body.appendChild(drop);
        
        drop.animate([
            { transform: 'translateY(0)', opacity: 1 },
            { transform: `translateY(${window.innerHeight}px)`, opacity: 0 }
        ], {
            duration: 2000 + Math.random() * 2000,
            easing: 'linear'
        });
        
        setTimeout(() => {
            if (drop.parentNode) {
                drop.parentNode.removeChild(drop);
            }
        }, 4000);
    }, 100);
}

// Добавляем CSS для частиц
const style = document.createElement('style');
style.textContent = `
    .blood-particle, .blood-explosion {
        position: fixed;
        pointer-events: none;
        z-index: 1000;
    }
    
    #blood-cursor {
        transition: all 0.3s;
    }
    
    @media (max-width: 768px) {
        #blood-cursor {
            display: none;
        }
    }
    
    body {
        cursor: none;
    }
    
    a, button {
        cursor: none;
    }
`;
document.head.appendChild(style);